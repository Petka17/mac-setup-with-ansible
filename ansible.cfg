[defaults]
# Explicit localhost inventory — avoids implicit-localhost warnings.
inventory = inventory.yml
# Friendlier output and no noisy interpreter-discovery warning for localhost.
stdout_callback = default
result_format = yaml
interpreter_python = auto_silent
nocows = true
