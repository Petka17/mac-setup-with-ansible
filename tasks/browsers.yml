---
- name: Install browsers
  # greedy: most of these casks are marked auto_updates, which `brew upgrade`
  # skips by default — so `state: latest` alone would never upgrade them after
  # the first install. greedy forces brew to own their updates.
  community.general.homebrew_cask:
    name: "{{ browsers }}"
    state: latest
    greedy: true
